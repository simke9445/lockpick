{
  "name": "lockpick",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.3.13",
  "description": "Standalone local advisory locking CLI and reusable TypeScript library.",
  "bin": {
    "lockpick": "./bin/lockpick.ts"
  },
  "files": [
    "bin/",
    "src/",
    "docs/readme-evidence.md",
    "README.md",
    "CONTRIBUTING.md",
    "LICENSE"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun run typecheck",
    "lockpick": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "doctor": "bun run src/index.ts doctor",
    "format": "biome format --write .",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run test && bun run typecheck && bun run lint"
  },
  "dependencies": {
    "commander": "14.0.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/bun": "1.3.13",
    "typescript": "6.0.3"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}
